apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: agentpulse-data
  namespace: agentpulse
  labels:
    app: agentpulse
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: local-path
