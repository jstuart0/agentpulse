export {};

// Placeholder — this file is removed by Phase 3's `git mv src/mcp
// packages/agentpulse-mcp/src` (see
// thoughts/shared/plans/2026-07-23-deliver-agentpulse-mcp-package.md).
// It exists only so the package's own tsconfig has at least one input file
// during Phases 1-2, when the package is wired into the root gate but the
// real MCP source hasn't moved yet.
